Hello <%= @resource.username.presence || @resource.email %>!

Someone requested a link to change your password. You can do this through the link below.

<%= @reset_password_url %>

If you didn't request this, please ignore this email. Your password won't change until you use the link above.
